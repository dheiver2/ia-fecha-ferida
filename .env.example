# =============================================
# BACKEND - Configurações do Servidor
# =============================================

# Servidor
PORT=3001
NODE_ENV=development

# Google Gemini AI (obrigatório)
# Obtenha em: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=sua_chave_gemini_aqui
GEMINI_MODEL_PRIORITY=gemini-2.5-pro,gemini-2.5-flash,gemini-flash-latest,gemini-pro-latest

# JWT - Autenticação (obrigatório em produção)
# Gere uma chave segura: openssl rand -base64 32
JWT_SECRET=sua_chave_jwt_super_secreta_aqui
JWT_EXPIRATION=24h

# Banco de Dados PostgreSQL (obrigatório)
# Exemplo: postgresql://user:pass@host:5432/dbname?sslmode=require
DATABASE_URL=postgresql://usuario:senha@localhost:5432/fecha_ferida

# Upload de arquivos
MAX_FILE_SIZE=10485760
UPLOAD_PATH=./uploads

# Segurança
BCRYPT_ROUNDS=12
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# CORS (produção)
FRONTEND_URL=http://localhost:8080
ALLOWED_ORIGINS=http://localhost:8080,http://localhost:5173

# =============================================
# FRONTEND - Configurações da Aplicação
# =============================================

# URL da API Backend
VITE_API_URL=http://localhost:3001

# Configurações da aplicação
VITE_APP_NAME=Fecha Ferida IA
VITE_APP_VERSION=1.0.0
