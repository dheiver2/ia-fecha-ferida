# Fecha Ferida IA - Environment Variables Example
# Copy this file to .env and fill with your actual values

# This file contains examples for both backend and frontend environment variables
# Copy the respective sections to backend/.env and frontend/.env

# =============================================================================
# BACKEND ENVIRONMENT VARIABLES (copy to backend/.env)
# =============================================================================

# Server Configuration
PORT=3001
NODE_ENV=development

# Google Gemini AI Configuration
GEMINI_API_KEY=your_gemini_api_key_here

# JWT Configuration
JWT_SECRET=your_super_secure_jwt_secret_here_minimum_32_characters
JWT_EXPIRES_IN=24h

# Database Configuration
DB_PATH=./database/fecha_ferida.db

# File Upload Configuration
MAX_FILE_SIZE=10485760
UPLOAD_PATH=./uploads

# Security Configuration
BCRYPT_ROUNDS=10
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# CORS Configuration (for production)
ALLOWED_ORIGINS=http://localhost:8080,http://localhost:3000

# =============================================================================
# FRONTEND ENVIRONMENT VARIABLES (copy to frontend/.env)
# =============================================================================

# API Configuration
VITE_API_URL=http://localhost:3001

# App Configuration
VITE_APP_NAME=Fecha Ferida IA
VITE_APP_VERSION=1.0.0

# =============================================================================
# PRODUCTION NOTES
# =============================================================================

# For production deployment:
# 1. Change NODE_ENV to 'production'
# 2. Use a strong JWT_SECRET (generate with: openssl rand -base64 32)
# 3. Set VITE_API_URL to your production API URL
# 4. Configure ALLOWED_ORIGINS with your production domains
# 5. Increase BCRYPT_ROUNDS to 12 for better security
# 6. Get a valid GEMINI_API_KEY from Google AI Studio